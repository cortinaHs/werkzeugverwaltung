version: '3.8'
services:
  web:
    container_name: werkzeugverwaltung_web
    build:
      context: ./
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development